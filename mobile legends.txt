class Player:
    def __init__(self, games, wins):
        self.games = games
        self.wins = wins

    def get_win_rate(self):
        win_rate = (self.wins / self.games) * 100
        return int(win_rate)

player = Player(130, 70)

win_rate = player.get_win_rate()

print(win_rate)